target-version = "py311"

[lint]
select = ["E4", "E7", "E9", "F", "I", "UP", "C4", "PIE", "BLE", "EXE"]

[lint.per-file-ignores]
# These three broad catches are deliberate gate boundaries that convert arbitrary
# check failures into structured CheckResult records. The remaining entries are
# temporary formatting debt isolated to this file and must not be expanded.
"scripts/pr_gate.py" = ["BLE001", "UP035", "C401", "PIE810"]
